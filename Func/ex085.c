#include<stdio.h>
void renketsu(char c1, char c2);

main()
{
	char a[256], b[256];
	printf("”z—ñ a:");
	gets(a);
	printf("”z—ñ b:");
	gets(b);
	renketsu(a, b);
	printf("”z—ñ a:%s",a)
}

void renketsu(char c1, char c2)
{
	int i, j;
	for (i = 0; *(s1 + i) != '\0'; i++);
	for (j = 0; *(s1 + i) = *(s2 + j); i++, j++);
	return;
}